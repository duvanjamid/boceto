{
  "buildCommand": "npm run build:lib && npm run build",
  "outputDirectory": "dist/wirescript/browser",
  "rewrites": [
    { "source": "/mcp", "destination": "/api/mcp" },
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
